# 凱薩加密
### **說明:** 凱薩加密是一種簡單的替換加密，將字母向後位移 $K$ 位。請撰寫一程式，輸入一個字串（僅含小寫字母）和一個整數 $K$。將字串中的每個字母向後移動 $K$ 個位置（超過 'z' 則繞回 'a'）。
*(提示：可利用 ord() 轉成 ASCII 碼運算，再用 chr() 轉回字母)*
## 輸入範例:
### abc
### $2$
## 輸出範例:
### cde